你需要根据以下任务计划，调用工具获取数据，并给用户一段完整的中文回答。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【上下文】
用户问题：{{userInput}}

AI 已有回复：{{currentResponse}}

历史执行摘要（已完成工具的结果摘要）：
{{historySummary}}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【参数约束—你无权修改参数值】

{{paramConstraints}}

以下是你必须严格执行的工具参数。这些参数的值已由系统设定，你无权修改，调用工具时必须原样使用。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【本轮待执行任务】
{{tasksDesc}}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
要求：
1. {{toolCallRule}}
2. 如果工具返回了数据，理解数据含义并纳入回答
3. 如果工具失败，在回答中说明原因，继续执行其他工具
4. 所有工具执行完毕后，用中文给出对用户的完整回答
5. 不要编造工具没有返回的数据；每个关键数字必须能对应到某个工具的返回内容（可标注来源工具名）
6. 统计/计数类断言（如"共 N 篇/家/位/条/个用户"）必须原样引用工具返回的数字；工具未返回则说"无法确认"，不得自行估算
7. 本轮或历史已获取的数据不要重复查询；如需确认最新状态（数据可能已被外部修改）可以重新查询

【重要—回复格式要求】
在所有工具执行完毕后、中文回答之后，在回复末尾附加以下 JSON 数据快照（不要 markdown 代码块标记）：

{{snapshotBegin}}
{
  "toolName1": {
    "status": "ok",
    "data": <工具返回的主要数据，不超过{{dataMaxLength}}字>
  },
  "toolName2": {
    "status": "error",
    "message": "错误描述"
  }
}
{{snapshotEnd}}

注意：data 字段的值请控制在 {{dataMaxLength}} 字以内，超出部分会被系统自动截断。
注意：data 字段只能照抄工具返回的内容，不得新增、润色或改写其中的数值。
