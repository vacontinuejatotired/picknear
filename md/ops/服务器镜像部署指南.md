# PickNear 服务器镜像部署指南（Pull 模式）

> **适用场景**：新服务器 / VPS / 演示机——**不构建镜像**，从阿里云 ACR 拉取现成镜像 + docker compose 部署。
> **前置**：构建机已把 `picknear-app`、`picknear-frontend` 镜像推到 ACR（构建/推送规范见 `Docker部署指南.md` §2，**必须 `--provenance=false --sbom=false`**）。
> 本 VM 的开发/构建/部署见 `vm-docs/VM部署交接文档.md`。

---

## 0. 部署拓扑与镜像清单

```
用户 ──48080──> picknear-frontend (nginx)
                │ /api → 反向代理 48082
                └──> picknear-app (Spring Boot, JVM ≤1G)
                      │        │         │
                  picknear-mysql  picknear-redis  picknear-rabbitmq
                (43307:3306)   (46379:6379)  (45672/55672)
                         全部容器在 picknear-net 网络内
```

| 服务 | 镜像 | 来源 | 容器内端口 | 宿主机端口 |
|---|---|---|---|---|
| app | `crpi-gbihc3e57h3o3f2l.cn-hangzhou.personal.cr.aliyuncs.com/picknear/picknear-app:latest` | **阿里云 ACR** | 8082 | 48082 |
| frontend | `crpi-gbihc3e57h3o3f2l.cn-hangzhou.personal.cr.aliyuncs.com/picknear/picknear-frontend:latest` | **阿里云 ACR** | 80 | 48080 |
| mysql | `mysql:8.0` | Docker Hub | 3306 | 43307 |
| redis | `redis:7-alpine` | Docker Hub | 6379 | 46379 |
| rabbitmq | `rabbitmq:4-management-alpine` | Docker Hub | 5672 / 15672 | 45672 / 55672 |

> 端口统一为「原端口 + 40000」，仅宿主机暴露侧改，容器内服务间仍走内部端口。

---

## 1. 服务器准备

| 项 | 要求 |
|---|---|
| Docker | Engine ≥20.10 + compose 插件（`docker compose version` 可用） |
| 内存 | ≥5G（容器上限合计 ~2.9G）；swap ≥2G |
| 磁盘 | ≥30G（实际用量 ~10G） |

**镜像加速器**（可选，但拉 mysql/redis/rabbitmq 官方镜像强烈建议）：

```bash
sudo tee /etc/docker/daemon.json >/dev/null <<'JSON'
{
  "registry-mirrors": [
    "https://docker.m.daocloud.io",
    "https://docker.1panel.live"
  ]
}
JSON
sudo systemctl restart docker
```

---

## 2. 登录阿里云 ACR

```bash
docker login --username=nick4768463442 crpi-gbihc3e57h3o3f2l.cn-hangzhou.personal.cr.aliyuncs.com
```

> 密码 = **阿里云容器镜像服务 → 访问凭证页设置的 registry 密码**（不是账号登录密码）。非交互环境用 `--password-stdin` 从 stdin 读。

---

## 3. 准备部署目录与配置文件

**保持与共享文件夹版一致的相对布局**（compose 里的 `../../` 前端路径才能直接用）：

```
<部署根>/                        # 例：/opt/picknear
├── picknear/
│   ├── docker-compose.yml       # ① 编排文件
│   ├── .env                     # ② 密钥 + 全部配置（统一走环境变量，§4）
│   └── src/main/resources/db/
│       └── heima-init.sql       # ③ MySQL 首次初始化脚本
└── nginx-1.18.0heima/
    └── frontend/
        └── nginx.conf           # ④ 前端 nginx 配置
```

从构建机/工作区（`E:\heima` 或 `/mnt/hgfs/heima`）拷贝这 4 项，保持相对路径：

```bash
mkdir -p <部署根>/{picknear/src/main/resources/db,nginx-1.18.0heima/frontend}

scp picknear/picknear/docker-compose.yml                   root@<服务器>:/opt/picknear/picknear/
scp picknear/picknear/.env                                 root@<服务器>:/opt/picknear/picknear/
scp picknear/picknear/src/main/resources/db/heima-init.sql root@<服务器>:/opt/picknear/picknear/src/main/resources/db/
scp nginx-1.18.0heima/frontend/nginx.conf                  root@<服务器>:/opt/picknear/nginx-1.18.0heima/frontend/
```

> ⚠️ 服务器上**不需要** Dockerfile / src / 前端源码（构建源），`--no-build` 直接用镜像。
> 若部署根不是 `/opt/picknear`，保持上面两层相对结构即可，compose 里的 `../../` 不受影响。

---

## 4. 配置 .env

compose 从 **compose 文件同目录**的 `.env` 读取变量。全部必填（`DB_PASSWORD` 缺失 compose 直接报错；**LANGFUSE 缺失 app 启动报 `Could not resolve placeholder`**）：

```bash
cd /opt/picknear/picknear && cp .env.example .env
```

```ini
# 必填
DB_PASSWORD=281458                     # 必须与 heima-init.sql 或现有库一致
OSS_ACCESS_KEY_ID=你的AccessKeyId
OSS_ACCESS_KEY_SECRET=你的AccessKeySecret
DASHSCOPE_API_KEY=sk-ws-你的Key          # 通义千问 AI

# 必填：Langfuse 云观测（app 启动必需，缺失会崩）
LANGFUSE_BASE_URL=https://cloud.langfuse.com
LANGFUSE_PUBLIC_KEY=你的pk-lf-...
LANGFUSE_SECRET_KEY=你的sk-lf-...
LANGFUSE_BASIC_AUTH=你的base64(pk:sk)

# 可选：日志级别（缺省 INFO）
# LOG_LEVEL_HMDP=INFO
# LOG_LEVEL_AGENT=INFO
```

> `OSS_ENDPOINT`/`bucket`/`region` 已硬编码在 `application-prod.yaml`，无需配置。`.env` 含密钥，**不要提交进 git**。

---

## 5. 拉取并启动

```bash
cd /opt/picknear/picknear

# 语法校验（可选）
docker compose config

# 拉取全部镜像（app/frontend 走 ACR，依赖走 Docker Hub/加速器）
docker compose pull

# 启动：--no-build 强制用已拉镜像，避免触发构建
docker compose up -d --no-build
```

> 首次启动会创建 MySQL 数据卷并执行 `heima-init.sql` 初始化。启动顺序有依赖：MySQL→Redis→RabbitMQ 逐个 `healthy` → app → frontend。

---

## 6. 验证

```bash
docker compose ps                    # 期望 5 个容器全部 Up (healthy)
curl -I http://localhost:48080       # 前端 200
curl http://localhost:48080/api/shop-type/list   # 业务链路通
```

浏览器访问：`http://<服务器IP>:48080`（外部访问需放行 48080）。

---

## 7. 更新流程（代码改动后）

构建机 push 新镜像 → 服务器拉取替换（同 tag 自动替换）：

```bash
# 构建机（见 Docker部署指南.md §2.3）
cd /e/heima/picknear/picknear
docker buildx build --provenance=false --sbom=false -t picknear-app:latest .
docker push crpi-gbihc3e57h3o3f2l.cn-hangzhou.personal.cr.aliyuncs.com/picknear/picknear-app:latest

# 服务器
cd /opt/picknear/picknear
docker compose pull
docker compose up -d --no-build
```

---

## 8. 常见问题

| 现象 | 原因 | 解决 |
|---|---|---|
| `docker compose up` 报 `required variable DB_PASSWORD is missing` | `.env` 缺失或变量为空 | 确认 `/opt/picknear/picknear/.env` 存在且有值（§4） |
| `unauthorized: authentication required` | 未登录私有仓库 | 重新 `docker login`（§2） |
| 拉官方镜像超时 | 加速器未生效 | 检查 daemon.json → `systemctl restart docker` |
| app 崩溃 `Could not resolve placeholder 'LANGFUSE_BASE_URL'` | `.env` 缺 LANGFUSE 4 个变量 | 补上真实 Langfuse 云值（§4） |
| app 一直不 healthy | 依赖未就绪 / DB 密码不一致 | `docker compose logs app`，先等依赖 healthy |
| 前端 502 | nginx.conf 挂载路径不对 / 后端未起 | 检查 `/etc/nginx/conf.d/default.conf`，`nginx -s reload` |
| 前端容器 `not a directory: mount a directory onto a file` | compose 前端挂载路径层级不对 | 确认部署根保持了 §3 的两层相对布局（`../../`） |
| 端口冲突 | 宿主机已有 8080 等占用 | compose 已用非常用端口（48080/48082/43307/46379/45672/55672） |

---

*创建：2026-08-05 · 镜像 tag：`picknear-app:latest` / `picknear-frontend:latest` · 端口：原端口 + 40000*
