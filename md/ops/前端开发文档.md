# 探点（picknear）前端开发文档

> 后端项目基于 Spring Boot 3.4.4，Java 17  
> 最后更新：2026-07（第2轮优化）

---

## 目录

1. [项目概述](#1-项目概述)
2. [基础信息](#2-基础信息)
3. [统一响应格式](#3-统一响应格式)
4. [认证机制（双Token）](#4-认证机制双token)
5. [API 接口列表](#5-api-接口列表)
   - [5.1 用户模块](#51-用户模块)
   - [5.2 商铺模块](#52-商铺模块)
   - [5.3 商铺类型模块](#53-商铺类型模块)
   - [5.4 探店笔记模块](#54-探店笔记模块)
   - [5.5 关注模块](#55-关注模块)
   - [5.6 优惠券模块](#56-优惠券模块)
   - [5.7 秒杀订单模块](#57-秒杀订单模块)
   - [5.8 文件上传模块](#58-文件上传模块)
   - [5.9 聊天模块](#59-聊天模块)
   - [5.10 博客评论模块](#510-博客评论模块)
   - [5.11 测试/工具接口](#511-测试工具接口)
6. [数据模型](#6-数据模型)
7. [关键业务逻辑说明](#7-关键业务逻辑说明)
8. [附录：HTTP 状态码说明](#8-附录http-状态码说明)
   - [9. 图片上传前端交互注意事项](#9-图片上传前端交互注意事项)
   - [9.1 图片选择器 UI 建议](#91-图片选择器-ui-建议)

---

## 1. 项目概述

**探点**是一个类大众点评的本地生活服务平台，核心功能包括：

- 用户注册/登录（手机验证码 + 密码）
- 商铺浏览（按类型、按名称、按地理位置）
- 探店笔记（发布、点赞、查看点赞用户、关注者动态流）
- 关注/取关/共同关注
- 优惠券查看与秒杀抢购
- 用户签到

### 技术栈

| 组件 | 技术 |
|------|------|
| 后端框架 | Spring Boot 3.4.4 |
| 数据库 | MySQL (MyBatis-Plus) |
| 缓存 | Redis + Caffeine 本地缓存 |
| 消息队列 | RabbitMQ |
| 认证 | JWT 双Token（Access Token + Refresh Token） |
| ID生成 | 基于 Redis 的雪花算法 |
| 跨域 | CorsFilter 全局配置（开发期允许所有来源） |

---

## 2. 基础信息

### 2.1 开发环境

```yaml
服务端口: 8082
上下文路径: /
基础URL: http://localhost:8082
```

### 2.2 全局配置说明

- JSON 序列化忽略 `null` 字段（`spring.jackson.default-property-inclusion: non_null`）
- MyBatis-Plus 实体别名包：`com.hmdp.entity`
- 分页默认大小：5 条/页（`DEFAULT_PAGE_SIZE`），最大 10 条/页（`MAX_PAGE_SIZE`）

### 2.3 跨域配置

项目已配置全局 CORS，开发期允许所有来源跨域访问：

```yaml
# 跨域默认行为：
# - 允许 Credentials（Cookie / Authorization 头）
# - 允许所有 Origin（生产环境应限制具体域名）
# - 允许所有 Method（GET/POST/PUT/DELETE/OPTIONS）
# - 暴露 authorization 响应头给前端（全小写）
# - 暴露 Refresh-Token 响应头给前端
# - Refresh-Token 通过 Set-Cookie 存入 httpOnly Cookie
```

前端开发时无需额外配置代理即可调用后端 API（端口 8082）。

### 2.4 公共请求头

| 请求头 | 说明 | 必填 |
|--------|------|------|
| `authorization` | `Bearer <access_token>`（JWT） | 大多数接口需要 |

> **注意**：`Refresh-Token` **不放在请求头**，由后端设为 httpOnly Cookie（JS 不可读），浏览器自动携带。

> **注意**：认证接口（登录、发验证码）和公开接口（商铺列表、热门笔记）不需要 Token。详见 [认证机制](#4-认证机制双token)。

---

## 3. 统一响应格式

所有接口返回统一的 `Result` 对象：

```json
{
  "success": true,
  "errorMsg": null,
  "data": { ... },
  "total": null,
  "code": 200
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `success` | Boolean | 请求是否成功 |
| `errorMsg` | String | 失败时的错误消息 |
| `data` | Object | 响应数据（可能为List、Object或null） |
| `total` | Long | 分页场景下的总数（当前未使用） |
| `code` | Integer | 统一业务状态码（**可选字段**：`200` 成功；`4xx` 客户端错误（400 参数/401 未登录/403 无权/404 不存在）；`5xx` 服务端错误（500 服务器异常）。未指定时为 null/缺失，与旧版响应一致） |

### 成功示例

```json
// GET /shop-type/list
{
  "success": true,
  "errorMsg": null,
  "data": [
    {
      "id": 1,
      "name": "美食",
      "icon": "/types/1.png",
      "sort": 1
    }
  ],
  "total": null
}
```

### 失败示例

```json
// POST /user/login 验证码错误
{
  "success": false,
  "errorMsg": "验证码错误",
  "data": null,
  "total": null
}
```

### 全局异常

未捕获的 RuntimeException 统一返回：

```json
{
  "success": false,
  "errorMsg": "服务器异常",
  "data": null,
  "total": null
}
```

---

## 4. 认证机制（双Token）

### 4.1 概述

项目使用 **Access Token + Refresh Token 双Token** 机制：

| Token | 有效期 | 作用 |
|-------|--------|------|
| `authorization`（Access Token） | 30 分钟 | 携带在请求头中，证明用户身份。**强制全小写** |
| `Refresh-Token` | 7 天 | 当 Access Token 过期时，用于刷新新 Token |

### 4.2 登录流程

```
前端                                   后端
  |                                     |
  |  1. POST /user/code?phone=xxx       |  发送验证码
  |------------------------------------->|
  |                                     |
  |  2. POST /user/login                |  登录（验证码或密码）
  |  { phone, code } 或 { phone, password }  |
  |<-------------------------------------|
  |  响应头：                            |
  |  authorization: Bearer <Access Token>|  ← 全小写
  |  Set-Cookie: refresh_token=<...>    |  ← httpOnly，JS 不可读
  |   （HttpOnly; Secure; SameSite=None）|
  |                                     |
  |  3. 前端保存 Access Token，Refresh   |
  |     Token 由浏览器 Cookie 自动管理   |
```

**密码登录说明**：
- `code` 和 `password` 二选一，同时传时优先使用 `code`
- 密码要求：至少 8 位，含大写字母 + 小写字母 + 数字
- 密码错误超过 10 次 → 账户锁定 30 分钟，返回 `"账户已锁定，请 30 分钟后重试"`
- 输入错误提示统一为 `"账号或密码错误"`（不区分用户是否存在，防止枚举攻击）

### 4.3 请求认证

登录后的所有需要登录的接口，请求头中必须携带：

```
authorization: Bearer <Access Token>
```

> **注意**：
> - **强制全小写** `authorization`，前后端统一
> - `Refresh-Token` 不放在请求头，由浏览器通过 httpOnly Cookie 自动携带
> - 响应头中**也会返回新的** `authorization`，前端每次请求后都需要用响应头中的值更新本地存储的 Access Token（因为拦截器可能会自动续期）
> - 跨域请求需配置 `withCredentials: true`，否则 Cookie 不会被发送

### 4.4 Token 自动续期机制

后端有一个 `RefreshTokenInterceptor` 拦截器，会在每次请求时做以下事情：

1. **校验 Access Token** 是否有效
2. **已过期** → 从 Cookie 读取 Refresh Token 尝试刷新（原子操作，Lua 脚本保证）
3. **刷新成功** → 响应头返回新的 `authorization: Bearer <token>` + `Set-Cookie` 更新 Refresh Token
4. **锁被占用**（并发刷新）→ 跳过刷新，响应头带 `X-Token-Refresh: skipped`，前端保持现有 Token
5. **两者都无效** → 返回 `401 Unauthorized` + `X-Token-Refresh: failed`，前端应跳转到登录页

> **响应头 `X-Token-Refresh`** 用于标识本次请求的 Token 刷新状态：
> - `ok` — 刷新成功，`authorization` 头中有新 Token
> - `skipped` — 刷新跳过（并发），无新 Token，前端保持现有 Token
> - `failed` — 刷新失败，伴随 401

### 4.5 前端 Token 管理最佳实践

```typescript
// 1. 全局配置：跨域携带 Cookie（SameSite=None 需要 withCredentials）
axios.defaults.withCredentials = true;

// 2. 请求拦截器
axios.interceptors.request.use(config => {
    const accessToken = localStorage.getItem('access_token');
    if (accessToken) {
        config.headers['authorization'] = `Bearer ${accessToken}`;
    }
    // Refresh-Token 由浏览器通过 httpOnly Cookie 自动携带，前端无需处理
    return config;
});

// 3. 响应拦截器
axios.interceptors.response.use(
    response => {
        const newAccess = response.headers['authorization'];  // Axios headers 是对象，不能用 .get()
        if (newAccess) {
            localStorage.setItem('access_token', newAccess.replace('Bearer ', ''));
        }
        // Refresh-Token 由 Set-Cookie 自动管理
        return response;
    },
    error => {
        if (error.response?.status === 401) {
            localStorage.removeItem('access_token');
            window.location.href = '/login';
        }
        return Promise.reject(error);
    }
);
```

### 4.6 需要登录的接口

以下接口**不需要**登录（公开接口）：

| 接口 | 说明 |
|------|------|
| `POST /user/code` | 发送验证码 |
| `POST /user/login` | 登录 |
| `GET /shop-type/list` | 商铺类型列表 |
| `GET /shop/**` | 所有商铺相关接口 |
| `GET /voucher/**` | 所有优惠券相关接口 |
| `GET /blog/hot` | 热门笔记列表 |

其余接口都需要携带 Token。

### 4.7 获取当前登录用户信息

登录后可直接通过 `GET /user/me` 获取：

```json
{
  "success": true,
  "data": {
    "id": 1,
    "nickName": "user_xxx",
    "icon": ""
  }
}
```

---

## 5. API 接口列表

### 5.1 用户模块

**Base URL**: `/user`

#### 5.1.1 发送手机验证码

```
POST /user/code?phone={phone}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `phone` | String | 是 | 手机号码 |

**响应**: `Result`（data 为验证码，仅开发环境返回，生产环境 data 为 null）

```json
// 开发环境响应示例
{
  "success": true,
  "data": "123456"
}
```

> **注意**：开发环境直接返回验证码数字，方便调试。生产环境 data 为 null（验证码仅通过短信发送）。

#### 5.1.2 登录

```
POST /user/login
Content-Type: application/json

{
  "phone": "13800138000",
  "code": "123456",    // 验证码登录时提供
  "password": "xxx"    // 密码登录时提供
}
```

> **注意**：`code` 和 `password` 二选一。验证码登录优先使用 `code`，密码登录使用 `password`。

**请求体字段**:

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `phone` | String | 是 | 手机号码 |
| `code` | String | 否 | 验证码 |
| `password` | String | 否 | 密码 |

**响应头**（成功时）:

```
authorization: Bearer eyJhbGciOiJSUzI1NiJ9...
Set-Cookie: refresh_token=...; HttpOnly; Secure; SameSite=None; Path=/
```

**响应体**: `Result`（成功时 data 为 null）

> 前端从 `Authorization` 响应头获取 Access Token，Refresh Token 由 Set-Cookie 自动管理。

#### 5.1.3 获取当前用户信息

```
GET /user/me
```

**响应**:

```json
{
  "success": true,
  "data": {
    "id": 1,
    "nickName": "user_a1b2c3",
    "icon": ""
  }
}
```

> 需要登录。用户名为 "user_" + 随机字符串。
> `nickName` 和 `icon` 从 `tb_user_info` 表获取（已从 `tb_user` 迁移至此），通过 Redis+Caffeine 缓存加速。

#### 5.1.4 根据ID查询用户

```
GET /user/{id}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | Long | 是 | 用户ID |

**响应**: `Result`，data 为 `UserDTO`

```json
{
  "success": true,
  "data": {
    "id": 1,
    "nickName": "user_a1b2c3",
    "icon": ""
  }
}
```

#### 5.1.5 查询用户详细信息

```
GET /user/info/{id}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | Long | 是 | 用户ID |

> **注意**：需要登录，且仅允许查询自己的信息。若传入其他用户 ID 返回 `401` + `"无权访问该用户信息"`。

**响应**: `Result`，data 为 `UserInfo` 对象（不含 createTime/updateTime）

```json
{
  "success": true,
  "data": {
    "userId": 1,
    "city": "上海",
    "introduce": "这个人很懒，什么都没留下",
    "fans": 0,
    "followee": 0,
    "gender": true,
    "birthday": null,
    "credits": 0,
    "level": false
  }
}
```

#### 5.1.6 用户签到

```
POST /user/sign
```

> 需要登录。使用 Redis BitMap 实现，以当月为周期。

**响应**: `Result`

#### 5.1.7 获取签到次数（本月）

```
GET /user/sign/count
```

> 需要登录。

**响应**:

```json
{
  "success": true,
  "data": 5   // 本月签到天数
}
```

#### 5.1.8 登出

```
POST /user/logout
```

> 需要登录。登出后后端会删除 Redis 中的 Token 和 Version 记录，原 Token 立即失效。

**响应**: `Result`（成功时 data 为 null）

#### 5.1.9 修改密码

```
PUT /user/password
Content-Type: application/json
Authorization: <token>

{
  "oldPassword": "Abc12345",
  "newPassword": "Xyz67890"
}
```

> 需要登录。依赖 RefreshTokenInterceptor 前置鉴权。

**密码要求**：至少 8 位，含大写字母 + 小写字母 + 数字。

**响应头**（成功时）:
```
authorization: Bearer <新 Access Token>      ← 旧 Token 已作废
Set-Cookie: refresh_token=<新 Refresh Token>; HttpOnly; Secure; SameSite=None; Path=/
```

> 修改密码后 version 自动 bump，前端需用响应头中的新 Access Token 替换本地存储的旧 Token。Refresh Token 由 Set-Cookie 自动更新。

#### 5.1.9.1 重置密码（免旧密码）

```
PUT /user/password/reset
Content-Type: application/x-www-form-urlencoded

phone=13800000000&code=123456&newPassword=Test1234
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| `phone` | String | 是 | 手机号 |
| `code` | String | 是 | 短信验证码（先调 `POST /user/code` 获取） |
| `newPassword` | String | 是 | 新密码（至少 8 位，含大写 + 小写 + 数字） |

> 不需要登录，不需要旧密码。用短信验证码验证身份。
>
> 常见场景：用户忘记密码 → 发验证码 → 重置。

**响应**: `Result`

```json
{
  "success": true
}
```

#### 5.1.10 编辑个人资料

```
PUT /user/profile
Content-Type: multipart/form-data
Authorization: <token>

# 文本字段（可选）
nickName: 新昵称
city: 上海
introduce: 新的个人简介
# 头像文件（可选，上传后自动存入 OSS/icons/）
iconFile: <图片文件>
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `iconFile` | MultipartFile | 否 | 头像图片文件（仅限 jpg/png/gif/webp，≤2MB） |
| `nickName` | String | 否 | 用户昵称（不传则不修改） |
| `city` | String | 否 | 所在城市（不传则不修改） |
| `introduce` | String | 否 | 个人简介（不传则不修改） |

> **说明**：
> - 所有字段均为可选，只传需要修改的字段即可
> - `nickName` 不允许为空字符串或纯空白
> - 头像文件上传至 `icons/` 目录（与博客图片共用 `FileService`，dev 走本地，prod 走 OSS）
> - 更换头像时后端自动删除旧头像文件
> - 前端可用 `FileReader` 实现上传前本地预览
> - 更新成功后，`GET /user/me` 和 `GET /user/{id}` 将立即返回新数据

**响应**: `Result`

```json
{
  "success": true
}
```

---

### 5.2 商铺模块

**Base URL**: `/shop`

#### 5.2.1 根据ID查询商铺

```
GET /shop/{id}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | Long | 是 | 商铺ID |

**响应**: `Result`，data 为 `Shop` 对象

```json
{
  "success": true,
  "data": {
    "id": 1,
    "name": "XX火锅店",
    "typeId": 1,
    "images": "/images/1.jpg,/images/2.jpg",
    "area": "陆家嘴",
    "address": "上海市浦东新区...",
    "x": 121.5,
    "y": 31.2,
    "avgPrice": 100,
    "sold": 500,
    "comments": 200,
    "score": 45,
    "openHours": "10:00-22:00",
    "createTime": "2026-01-01T00:00:00",
    "updateTime": "2026-01-01T00:00:00",
    "distance": null
  }
}
```

> `distance` 字段仅在使用地理坐标查询时不为 null，表示距离（单位：米/千米）。

#### 5.2.2 按类型分页查询商铺（支持动态排序）

```
GET /shop/of/type?typeId={typeId}&current={current}&x={x}&y={y}&sortBy=score
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| `typeId` | Integer | 是 | 商铺类型ID |
| `current` | Integer | 否 | 页码，默认 1，每页 5 条 |
| `x` | Double | 否 | 经度（用于按距离排序） |
| `y` | Double | 否 | 纬度（用于按距离排序） |
| `sortBy` | String | 否 | 排序字段：`score`（评分）、`sold`（销量）、`comments`（评论数）、`avgPrice`（均价） |

> - 不传 `sortBy` 且不传 `x`/`y` → 默认顺序（按 id）
> - 传了 `x`/`y` → **按距离排序**（`sortBy` 无效，GEO 优先）
> - 传了 `sortBy` 但没传 `x`/`y` → **按指定字段降序排列**
> - `distance` 字段仅在使用地理坐标查询时不为 null

**响应**: `Result`，data 为 `Shop[]`

```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "name": "xx茶馆",
      "typeId": 1,
      "images": "http://...",
      "area": "陆家嘴",
      "address": "xx路xx号",
      "x": 120.12,
      "y": 30.28,
      "avgPrice": 80,
      "sold": 256,
      "comments": 128,
      "score": 45,
      "distance": 1.2
    }
  ]
}
```

#### 5.2.3 按名称分页查询商铺

```
GET /shop/of/name?name={name}&current={current}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | String | 否 | 商铺名称关键字（模糊搜索） |
| `current` | Integer | 否 | 页码，默认 1，每页 10 条（`MAX_PAGE_SIZE`） |

#### 5.2.4 新增商铺

```
POST /shop
Content-Type: application/json

{
  "name": "新商铺",
  "typeId": 1,
  "images": "...",
  "area": "...",
  "address": "...",
  "x": 121.5,
  "y": 31.2,
  "avgPrice": 100,
  "openHours": "10:00-22:00"
}
```

**响应**: `Result`，data 为新商铺的 ID

#### 5.2.5 更新商铺信息

```
PUT /shop
Content-Type: application/json

{
  "id": 1,
  "name": "更新后的名字",
  ...
}
```

**响应**: `Result`

#### 5.2.6 查询周边热门店铺（5km内）

```
GET /shop/near/{typeId}?x={x}&y={y}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `typeId` | Integer | 是 | 商铺类型ID（路径参数） |
| `x` | Double | 是 | 当前经度 |
| `y` | Double | 是 | 当前纬度 |

> 基于 Redis GEO 数据结构查询 5km 内的热门商铺，返回固定前 10 条，不含距离字段。
> 数据来源为独立的 GEO 集合 `shop:hot:{typeId}`，与 `of/type` 的距离排序互不干扰。
> 若周边无店铺则返回 `"周边暂无热门店铺"`。

**响应**: `Result`，data 为 `Shop` 数组

```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "name": "XX火锅店",
      "typeId": 1,
      "images": "/images/1.jpg",
      "area": "陆家嘴",
      "address": "上海市浦东新区...",
      "x": 121.5,
      "y": 31.2,
      "avgPrice": 100,
      "score": 45,
      "openHours": "10:00-22:00"
    }
  ]
}
```

---

### 5.3 商铺类型模块

**Base URL**: `/shop-type`

#### 5.3.1 获取商铺类型列表

```
GET /shop-type/list
```

**响应**: `Result`，data 为 `ShopType` 数组

```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "name": "美食",
      "icon": "/types/1.png",
      "sort": 1
    },
    {
      "id": 2,
      "name": "KTV",
      "icon": "/types/2.png",
      "sort": 2
    }
  ]
}
```

> 按 `sort` 字段升序排列。`createTime` 和 `updateTime` 被忽略（`@JsonIgnore`）。

---

### 5.4 探店笔记模块

**Base URL**: `/blog`

#### 5.4.1 发布笔记

```
POST /blog
Content-Type: application/json

{
  "shopId": 1,
  "title": "探店标题",
  "content": "探店内容描述..."
}
```

| 请求体字段 | 类型 | 必填 | 说明 |
|------------|------|------|------|
| `shopId` | Long | 是 | 关联的商铺ID |
| `title` | String | 是 | 笔记标题 |
| `content` | String | 否 | 文字描述 |

> **注意**：此接口**不接收** `images` 字段。图片上传走以下两步流程：

**完整发布流程**：

```
① POST /blog                  → 创建博客，返回 blogId
② POST /upload/blog?blogId=X  → 逐张上传图片（循环），返回完整 URL
③ PUT /blog/{id}/images       → 提交最终 images 列表（JSON 数组）
```

**响应**: `Result`，data 为 blogId

```json
{
  "success": true,
  "data": 123
}
```

#### 5.4.1.1 更新博客图片列表

上传完成后调用此接口提交最终图片列表。

```
PUT /blog/{id}/images
Content-Type: application/json

["url1", "url2", "url3"]
```

| 参数 | 位置 | 类型 | 必填 | 说明 |
|------|------|------|------|------|
| `id` | Path | Long | 是 | 博客ID |
| `images` | Body | String[] | 是 | 图片完整 URL 的 JSON 数组 |

> **注意**：
> - 以 **JSON 数组** 形式提交，避免逗号拼接 URL 时 URL 本身含逗号导致切分错误
> - 后端会将数组转为逗号分隔字符串存储，前端读取时仍用 `getImageUrl()` 处理
> - 只有博客作者本人才能更新

**响应**: `Result`

```json
{
  "success": true
}
```

#### 5.4.1.2 完整前端发布流程示例

```typescript
// ① 创建博客（不含图片），获取 blogId
const blogRes = await axios.post('/blog', {
    shopId: 1,
    title: '探店标题',
    content: '探店内容...'
});
const blogId = blogRes.data.data;

// ② 逐张上传图片
const imageUrls: string[] = [];
for (const file of selectedFiles) {
    const formData = new FormData();
    formData.append('file', file);
    const uploadRes = await axios.post(`/upload/blog?blogId=${blogId}`, formData);
    imageUrls.push(uploadRes.data.data);
}

// ③ 提交最终图片列表（JSON 数组）
await axios.put(`/blog/${blogId}/images`, imageUrls, {
    headers: { 'Content-Type': 'application/json' }
});
```

> **注意**：第③步以 **JSON 数组** 形式提交（`PUT` body 直接传数组），不再用 GET 参数逗号拼接。

#### 5.4.2 查询热门笔记（分页）

```
GET /blog/hot?current={current}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `current` | Integer | 否 | 页码，默认 1，每页 10 条 |

**响应**: `Result`，data 为 `Blog` 数组

```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "shopId": 1,
      "userId": 1,
      "icon": "/icons/user1.jpg",
      "name": "用户昵称",
      "isLike": true,
      "title": "探店标题",
      "images": "/blogs/0/0/xxx.jpg",
      "content": "描述...",
      "liked": 100,
      "comments": 20,
      "createTime": "2026-03-01T12:00:00",
      "updateTime": "2026-03-01T12:00:00"
    }
  ]
}
```

> **注意**：
> - `icon` 和 `name` 字段不是数据库字段，是查询时从 User 表补充的作者信息
> - `isLike` 字段表示当前登录用户是否已点赞（登录状态下才会判断，未登录为 null）
> - 按点赞数降序排列

#### 5.4.3 根据ID查询笔记详情

```
GET /blog/{id}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | Long | 是 | 笔记ID |

**响应**: `Result`，data 为 `Blog` 对象（包含作者信息和点赞状态）

> **缓存说明**：
> - 优先查询 Redis 缓存（key: `cache:blog:{id}`），未命中则查 MySQL 并回写 Redis
> - 缓存过期时间带随机偏移（30~60分钟），防止缓存雪崩
> - 若 MySQL 无此笔记，Redis 写入 2 分钟空值缓存，防止缓存穿透
> - `icon`、`name`（作者信息）和 `isLike`（点赞状态）为动态字段，每次查询实时填充，不缓存

#### 5.4.4 查询某个用户的所有笔记

```
GET /blog/of/user?id={userId}&current={current}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | Long | 是 | 用户ID |
| `current` | Integer | 否 | 页码，默认 1，每页 10 条 |

**响应**: `Result`，data 为 `Blog` 数组

#### 5.4.5 查询当前用户的笔记

```
GET /blog/of/me?current={current}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `current` | Integer | 否 | 页码，默认 1，每页 10 条 |

> 需要登录。

#### 5.4.6 点赞/取消点赞笔记

```
PUT /blog/like/{id}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | Long | 是 | 笔记ID |

> **逻辑**：若当前用户未点赞 → 点赞（liked+1）；若已点赞 → 取消点赞（liked-1）  
> 点赞数据存储在 Redis ZSet 中，按点赞时间排序。

**响应**: `Result`

#### 5.4.7 查询笔记点赞用户列表（Top 5）

```
GET /blog/likes/{id}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | Long | 是 | 笔记ID |

**响应**: `Result`，data 为 `UserDTO` 数组（最多 5 个）

```json
{
  "success": true,
  "data": [
    { "id": 2, "nickName": "user_xxx", "icon": "" },
    { "id": 3, "nickName": "user_yyy", "icon": "" }
  ]
}
```

> **注意**：`nickName` 和 `icon` 从 `tb_user_info` 表查询（已从 `tb_user` 迁移至此）。

#### 5.4.8 查询关注者的笔记动态（Feed流）

```
GET /blog/of/follow?lastId={max}&offset={offset}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `lastId` | Long | 是 | 上次查询的最小时间戳（首次传当前时间戳） |
| `offset` | Integer | 否 | 偏移量（用于处理相同时间戳的重复），默认 0 |

> **滚动分页说明**（基于 Redis ZSet）：
> - 首次请求：`lastId` 传当前时间戳（毫秒），`offset` 传 0
> - 后续请求：`lastId` 传上次响应中的 `minTime`，`offset` 传上次响应中的 `offset`
> - 直到返回的 list 为空表示没有更多数据

**响应**: `Result`，data 为 `ScrollResult` 对象

```json
{
  "success": true,
  "data": {
    "list": [ /* Blog数组 */ ],
    "minTime": 1710000000000,
    "offset": 2
  }
}
```

---

### 5.5 关注模块

**Base URL**: `/follow`

#### 5.5.1 关注/取关用户

```
PUT /follow/{id}/{isfollow}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | Long | 是 | 目标用户ID |
| `isfollow` | Boolean | 是 | `true` 关注，`false` 取消关注 |

> 需要登录。

**响应**: `Result`

#### 5.5.2 查询是否已关注

```
GET /follow/or/not/{id}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | Long | 是 | 目标用户ID |

**响应**:

```json
{
  "success": true,
  "data": true  // true=已关注, false=未关注
}
```

#### 5.5.3 查询共同关注

```
GET /follow/common/{id}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | Long | 是 | 目标用户ID |

**响应**: `Result`，data 为 `UserDTO` 数组

```json
{
  "success": true,
  "data": [
    { "id": 3, "nickName": "user_xxx", "icon": "" }
  ]
}
```

---

### 5.6 优惠券模块

**Base URL**: `/voucher`

#### 5.6.1 查询商铺的优惠券列表

```
GET /voucher/list/{shopId}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `shopId` | Long | 是 | 商铺ID |

**响应**: `Result`，data 为 `Voucher` 数组

```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "shopId": 1,
      "title": "50元代金券",
      "subTitle": "满100可用",
      "rules": "使用规则...",
      "payValue": 50,
      "actualValue": 100,
      "type": 1,
      "status": 1,
      "stock": 100,
      "beginTime": "2026-03-01T00:00:00",
      "endTime": "2026-03-31T23:59:59",
      "createTime": "2026-01-01T00:00:00"
    }
  ]
}
```

> **说明**：
> - `type`：1=普通券，2=秒杀券（秒杀券会有 `stock`、`beginTime`、`endTime` 字段）
> - `stock`、`beginTime`、`endTime` 不是 Voucher 表字段，是从 `tb_seckill_voucher` 关联查询的
> - `payValue` = 支付金额，`actualValue` = 抵扣金额

#### 5.6.2 新增普通优惠券

```
POST /voucher
Content-Type: application/json

{
  "shopId": 1,
  "title": "50元代金券",
  "subTitle": "满100可用",
  "rules": "仅限堂食",
  "payValue": 50,
  "actualValue": 100,
  "type": 1
}
```

**响应**: `Result`，data 为新优惠券 ID

#### 5.6.3 新增秒杀券

```
POST /voucher/seckill
Content-Type: application/json

{
  "shopId": 1,
  "title": "1元秒杀100元代金券",
  "subTitle": "限量100份",
  "rules": "每人限购1份",
  "payValue": 1,
  "actualValue": 100,
  "type": 2,
  "stock": 100,
  "beginTime": "2026-03-01T00:00:00",
  "endTime": "2026-03-31T23:59:59"
}
```

**响应**: `Result`，data 为新秒杀券 ID

---

### 5.7 秒杀订单模块

**Base URL**: `/voucher-order`

#### 5.7.1 秒杀下单 V2（推荐方式）

```
POST /voucher-order/seckill/{id}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | Long | 是 | 秒杀券ID |

> 需要登录。  
> **业务流程**：1. Redis Lua 脚本原子校验库存 + 校验重复下单 → 2. 发送 RabbitMQ 消息 → 3. 异步消费消息写入数据库

**响应**: `Result`

```json
// 秒杀成功
{ "success": true, "data": null }

// 库存不足
{ "success": false, "errorMsg": "库存不足" }

// 重复下单
{ "success": false, "errorMsg": "不允许重复下单" }
```

#### 5.7.2 秒杀下单 V1（旧版，直接落库）

```
POST /voucher-order/seckill/saveOrder/{id}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | Long | 是 | 秒杀券ID |

> 需要登录。直接写入数据库（性能不如 V2）。一般建议用 V2。

---

### 5.8 文件上传模块

**Base URL**: `/upload`

#### 5.8.1 上传图片

```
POST /upload/blog?blogId={blogId}
Content-Type: multipart/form-data

file: <图片文件>
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `file` | MultipartFile | 是 | 图片文件（仅限 jpg/png/gif/webp，≤5MB，≤4096×4096px） |
| `blogId` | Long | 是 | 所属笔记ID（先创建博客获取） |

> 建议前端在上传前校验文件类型、大小和尺寸，减少无效请求。后端额外通过 ImageIO 校验文件魔数以防范伪造扩展名。

**响应**: `Result`，data 为图片的完整访问 URL

```json
// 开发环境
{
  "success": true,
  "data": "http://localhost:8082/imgs/blogs/123/0/1/a1b2c3d4.jpg"
}

// 生产环境（OSS）
{
  "success": true,
  "data": "https://hm-dianping-images.oss-cn-hangzhou.aliyuncs.com/blogs/123/0/1/a1b2c3d4.jpg"
}
```

> **说明**：
> - data 为图片的**完整访问 URL**，前端直接使用，无需拼接域名
> - 图片路径包含 blogId 做目录分组：`blogs/{blogId}/{d1}/{d2}/{uuid}.{ext}`
> - 删除接口见 §5.8.2，传入此完整 URL 即可删除
> - 生产环境 OSS 域名需在 CORS 配置或 nginx 代理中允许前端跨域访问
> - 详细方案见 [`阿里云OSS图片上传方案.md`](./阿里云OSS图片上传方案.md)
> - 博客图片上传流程见 §5.4.1

#### 5.8.2 删除图片

```
DELETE /upload/blog/delete?url={url}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `url` | String | 是 | 上传时返回的完整图片 URL |

**响应**: `Result`

> 删除接口已从 `GET` 改为 `DELETE`，参数从 `name` 改为 `url`。存量前端页面需同步更新。

#### 5.8.3 存量老数据兼容

项目中存量图片路径为相对路径（如 `/imgs/blogs/...`），后端不负责 URL 转换，前端需统一处理。建议在项目中添加工具函数：

```typescript
// utils/image.ts — 图片地址兼容函数
export function getImageUrl(path: string | null | undefined): string {
    if (!path) return '';
    if (path.startsWith('http')) return path;      // 新数据：完整 URL
    return `http://localhost:8082${path}`;           // 存量老数据：拼接域名
}
```

**需要调用的字段**（必须全部覆盖）：

| 字段 | 示例值 | 调用位置 |
|------|--------|---------|
| `user.icon` | `/imgs/icons/user5-icon.png` | 用户头像 |
| `blog.icon` | `/imgs/icons/user5-icon.png` | 博客列表/详情中的博主头像 |
| `blog.images` | `/imgs/blogs/7/14/4771fefb-...jpg` | 博客中的图片展示 |
| `shop.images` | （逗号分隔的多张图） | 商铺详情页 |
| `shopType.icon` | （图片路径） | 商铺类型图标 |

> ⚠️ 若数据以逗号分隔多张图，需先 `.split(',')` 再对每张图调用 `getImageUrl()`。

---

### 5.9 聊天模块

**Base URL**: `/chat`

#### 5.9.1 发送聊天消息（未实现）

```
POST /chat?content={content}
```

**当前返回**: `{"success": true, "data": "聊天功能正在开发中..."}`

---

### 5.10 博客评论模块

**Base URL**: `/blog-comments`

> 当前 Controller 为空，暂无可用接口。数据库有 `tb_blog_comments` 表。

---

### 5.11 测试/工具接口

**Base URL**: `/test`

> 以下接口用于开发和压测，**前端一般不需要调用**。

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/test/costTime` | 模拟 500ms 耗时操作，返回耗时字符串 |
| GET | `/test/void` | 空方法测试，返回 `"void方法测试成功"` |
| POST | `/test/restart/{num}/{voucherId}` | 测试用：重置秒杀库存 |
| POST | `/test/generateToken/{num}?fileName=xxx` | 测试用：批量生成 Token 导出到 CSV |
| GET | `/test/checkSnowFlake/{num}` | 测试用：校验雪花ID是否重复 |
| GET | `/test/mq/order/save/{voucherId}/{orderNum}` | 测试用：批量发送秒杀订单消息到 MQ |

---

## 6. 数据模型

### 6.1 Result（统一响应）

```json
{
  "success": Boolean,
  "errorMsg": String | null,
  "data": Object | null,
  "total": Long | null,
  "code": Integer | null
}
```

> `code` 为统一业务状态码（可选，null 时不返回）：`200` 成功 / `400` 参数错误 / `401` 未登录或过期 / `403` 无权访问 / `404` 不存在 / `500` 服务器异常。

### 6.2 UserDTO（用户摘要）

```json
{
  "id": Long,
  "nickName": String,
  "icon": String
}
```

### 6.3 User（用户完整信息）

```json
{
  "id": Long,
  "phone": String,
  "password": String,
  "createTime": LocalDateTime,
  "updateTime": LocalDateTime
}
```

> `password` 字段查询时不会被序列化返回（经过加密处理），前端登录后获取的是 `UserDTO`。
> `nickName` 和 `icon` 已从 `tb_user` 表迁移至 `tb_user_info` 表，通过 `UserInfo` 对象返回。

### 6.4 UserInfo（用户详细信息）

```json
{
  "userId": Long,
  "nickName": String,
  "icon": String,
  "city": String,
  "introduce": String,
  "fans": Integer,
  "followee": Integer,
  "gender": Boolean,
  "birthday": LocalDate,
  "credits": Integer,
  "level": Boolean,
  "createTime": LocalDateTime,
  "updateTime": LocalDateTime
}
```

> `nickName` 和 `icon` 从 `tb_user` 迁移至此，`GET /user/me` 和 `GET /user/{id}` 返回的 `UserDTO` 数据即来源于此。

### 6.5 Shop（商铺）

```json
{
  "id": Long,
  "name": String,
  "typeId": Long,
  "images": String,
  "area": String,
  "address": String,
  "x": Double,
  "y": Double,
  "avgPrice": Long,
  "sold": Integer,
  "comments": Integer,
  "score": Integer,
  "openHours": String,
  "createTime": LocalDateTime,
  "updateTime": LocalDateTime,
  "distance": Double
}
```

> - `images`：多个图片路径以逗号 `,` 分隔
> - `score`：评分乘 10 保存（如 45 表示 4.5 分）
> - `distance`：非数据库字段，仅在使用地理坐标查询时填充

### 6.6 ShopType（商铺类型）

```json
{
  "id": Long,
  "name": String,
  "icon": String,
  "sort": Integer
}
```

> `createTime`、`updateTime` 字段存在但被 `@JsonIgnore` 注解，不会序列化返回。

### 6.7 Blog（探店笔记）

```json
{
  "id": Long,
  "shopId": Long,
  "userId": Long,
  "icon": String,
  "name": String,
  "isLike": Boolean,
  "title": String,
  "images": String,
  "content": String,
  "liked": Integer,
  "comments": Integer,
  "createTime": LocalDateTime,
  "updateTime": LocalDateTime
}
```

> - `icon`、`name`：非数据库字段，查询时从 UserInfo 表补充的作者信息（nickName + icon）
> - `isLike`：非数据库字段，表示当前登录用户是否已点赞
> - `images`：多张图片用逗号分隔（通过 `PUT /blog/{id}/images` 以 JSON 数组提交，后端存储为逗号分隔）

### 6.8 Voucher（优惠券）

```json
{
  "id": Long,
  "shopId": Long,
  "title": String,
  "subTitle": String,
  "rules": String,
  "payValue": Long,
  "actualValue": Long,
  "type": Integer,
  "status": Integer,
  "stock": Integer,
  "beginTime": LocalDateTime,
  "endTime": LocalDateTime,
  "createTime": LocalDateTime,
  "updateTime": LocalDateTime
}
```

> - `type`：1=普通券，2=秒杀券
> - `stock`、`beginTime`、`endTime`：非 Voucher 表字段，仅秒杀券时有值

### 6.9 SeckillVoucher（秒杀券库存）

```json
{
  "voucherId": Long,
  "stock": Integer,
  "beginTime": LocalDateTime,
  "endTime": LocalDateTime
}
```

### 6.10 VoucherOrder（秒杀订单）

```json
{
  "id": Long,
  "userId": Long,
  "voucherId": Long,
  "payType": Integer,
  "status": Integer,
  "createTime": LocalDateTime,
  "payTime": LocalDateTime,
  "useTime": LocalDateTime,
  "refundTime": LocalDateTime,
  "updateTime": LocalDateTime
}
```

> - `payType`：1=余额支付，2=支付宝，3=微信
> - `status`：1=未支付，2=已支付，3=已核销，4=已取消，5=退款中，6=已退款

### 6.11 Follow（关注关系）

```json
{
  "id": Long,
  "userId": Long,
  "followUserId": Long,
  "createTime": LocalDateTime
}
```

### 6.12 BlogComments（博客评论）

```json
{
  "id": Long,
  "userId": Long,
  "blogId": Long,
  "parentId": Long,
  "answerId": Long,
  "content": String,
  "liked": Integer,
  "status": Boolean,
  "createTime": LocalDateTime,
  "updateTime": LocalDateTime
}
```

> 当前该模块后台接口尚未实现（Controller 为空），仅数据库表已存在。

### 6.13 ScrollResult（滚动分页结果）

```json
{
  "list": [ /* 数据列表 */ ],
  "minTime": Long,
  "offset": Integer
}
```

### 6.14 LoginFormDTO（登录表单）

```json
{
  "phone": String,
  "code": String,
  "password": String
}
```

### 6.15 PasswordChangeDTO（修改密码请求体）

> 用于 `PUT /user/password` 接口，见 [§5.1.9](#519-修改密码)。

```json
{
  "oldPassword": String,
  "newPassword": String
}
```

### 6.16 ProfileUpdateDTO（编辑个人资料请求体）

> 用于 `PUT /user/profile` 接口，见 [§5.1.10](#5110-编辑个人资料)。所有字段均为可选，只传需要修改的字段。

```json
{
  "nickName": String,
  "icon": String,
  "city": String,
  "introduce": String
}
```

---

## 7. 关键业务逻辑说明

### 7.1 商铺列表按距离排序

调用 `GET /shop/of/type` 时，如果传入 `x`（经度）和 `y`（纬度），后端会基于 Redis GEO 数据结构按距离从近到远排序返回，结果中 `distance` 字段表示距离。

### 7.2 笔记 Feed 流（关注者动态）

- 用户发布笔记时，系统会将笔记 ID 推送给其所有粉丝的 **Redis ZSet**（key: `feed:{userId}`，score: 当前时间戳）
- 粉丝查看动态时，通过 `GET /blog/of/follow` 实现**滚动分页**
- 滚动分页参数 `lastId` 和 `offset` 用于处理同一毫秒多条数据的翻页问题

### 7.3 秒杀流程（V2 - MQ 异步模式）

```
用户请求 POST /voucher-order/seckill/{voucherId}
          │
          ▼
  Redis Lua 脚本原子执行：
    1. 检查库存是否充足 (key: seckill:stock:{voucherId})
    2. 检查用户是否已下单 (key: seckill:order:{voucherId})
    3. 扣减库存
    4. 记录用户已下单
          │
      ┌───┴───┐
    成功      失败
      │        └→ 返回错误信息
      ▼
  生成订单ID（雪花算法）
      │
      ▼
  发送到 RabbitMQ (queue: voucher_order)
      │
      ▼
  异步消费消息 → 写入 MySQL tb_voucher_order
      │
      ▼
  返回成功
```

### 7.4 点赞系统

- 点赞数据存储在 **Redis ZSet**（key: `blog:liked:{blogId}`，member: userId，score: 点赞时间戳）
- 查询 Top 5 点赞用户时，按点赞时间从早到晚取前 5 个
- 数据库中的 `liked` 字段实时同步更新

### 7.5 用户签到

- 使用 Redis BitMap，key: `sign:{userId}`，按月存储
- offset 为当月第几天（从 0 开始）

---

## 8. 附录：HTTP 状态码说明

| 状态码 | 说明 | 处理方式 |
|--------|------|---------|
| `200` | 请求成功 | 正常解析 `Result` 对象 |
| `401` | 未授权 / Token 无效或过期 | 清除本地 Token，跳转登录页 |
| `500` | 服务器内部错误 | 提示 "服务器异常"，联系后端排查 |

---

> **补充说明**：
> - 时间字段格式：`yyyy-MM-dd'T'HH:mm:ss`（ISO-8601，如 `2026-03-01T12:00:00`）
> - 所有需要登录的接口若未传入有效 Token，后端返回 `401` 状态码（无响应体）
> - 图片上传及路径兼容详见 §5.8

---

## 9. 图片上传前端交互注意事项

> 以下问题来自 2026-07 审查，建议在实现时优先考虑。

| # | 问题 | 级别 | 建议 |
|:-:|------|:----:|------|
| F1 | 无"取消发布/放弃草稿"功能，用户无法主动清理已上传的无效图片 | 🟢 P3 | 增加草稿管理 + 过期清理 |
| F2 | 上传失败无重试策略 | 🟢 P3 | 上传组件增加重试按钮 / 自动重试 |
| F3 | 发布中未禁用提交按钮，可能重复请求导致数据错乱 | 🟢 P3 | 提交后禁用按钮直到返回 |
| F4 | 图片选择器不支持多选，逐张选择效率低 | 🟢 P3 | 支持多选图片（不超过 9 张），逐张上传并显示进度 |

### 9.1 图片选择器 UI 建议

- **多选**：支持一次选择多张图片（不超过 9 张）
- **逐张上传**：循环调用 `POST /upload/blog?blogId={blogId}` 逐张上传，前端维护上传进度条
- **失败重试**：单张上传失败可点击重试，不影响其他图片
- **删除已传**：已上传的图片可在列表中预览并单独删除（调用 `DELETE /upload/blog/delete?url={url}` 接口）
- **禁用提交**：上传过程中禁用"发布"按钮，防止重复请求

---

